galaxy_info:
  author: Joe Nyland
  role_name: debian_backports
  namespace: joenyland
  description: Installs Debian backports
  license: MIT
  min_ansible_version: '2.1'
  platforms:
  - name: Debian
    versions:
      - bullseye # 12 (stable)
      - bullseye # 11 (oldstable)
      - buster # 10 (oldoldstable)
  galaxy_tags:
    - debian
    - backports
dependencies: []
